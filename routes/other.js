/**
 * @description: this code file is for routes that haven't been
 * decided yet and the work is still on.
 */

const express = require('express');

const router = express.Router();
const { protect } = require('../middlewares/authMiddleware');


